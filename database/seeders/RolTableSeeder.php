<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class RolTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        /*DB::table('rol')->truncate();
        Rol::truncate();
        for($i = 0; $i<30; $i++) {
            $rol_adi = rrim($faker->sentence(1), '.');

            Rol::create([
                'rol_adi' => $rol_adi
            ]);
        }
        */
    }
}
